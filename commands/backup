#!/bin/bash

help() {
  echo "Usage:"
  echo "  service backup"
}

core() {
  service::backup "$@"
}

run() {
  if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi

  core "$@"
}

run "$@"
